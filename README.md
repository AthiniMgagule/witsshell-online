# witsshell-online
A web-based terminal emulator built with React that simulates Unix shell functionality. Originally implemented in C with process forking and system calls, then reimagined as an interactive browser application with full command parsing, file system simulation, and environment variable support.
